# Agent Workflow - Zapomni Project

## 🎯 ГЛАВНЫЙ ПРИНЦИП

**Claude Code (я) работаю как менеджер проекта, делегируя задачи специализированным агентам через Task tool.**

Я НЕ делаю работу сам - я управляю агентами, координирую их работу и докладываю результаты пользователю.

### ⚠️ КРИТИЧНО ДЛЯ МЕНЯ (главного агента):

**Я ВСЕГДА использую sequential-thinking когда:**
- Формирую план для сложной задачи
- Анализирую отчеты от нескольких агентов
- Принимаю решение о рисках (архитектура, миграции данных, breaking changes)
- Выбираю между несколькими подходами
- Планирую параллелизацию агентов

**Пример:**
```
Пользователь: "Нужно оптимизировать систему памяти для AI-агентов"

Я ДЕЛАЮ:
1. sequential-thinking:
   - Какие проблемы с текущей реализацией?
   - Vector DB vs Graph DB vs гибридный подход?
   - Какие риски у каждого решения?
   - Как это повлияет на производительность?

2. Формирую план из 5-7 шагов

3. Запускаю агента с четким планом
```

**БЕЗ sequential-thinking → хаотичная работа → больше ошибок**

---

## 🧠 ОБЯЗАТЕЛЬНЫЕ ИНСТРУМЕНТЫ

### Sequential Thinking (для меня и агентов)

**Я (главный агент) ВСЕГДА использую:**
- `mcp__sequential-thinking__sequentialthinking` для сложных решений
- Перед формированием плана
- Перед принятием критичных решений
- Для анализа рисков

**Агенты должны использовать:**
- При диагностике сложных проблем (>3 возможных причин)
- При планировании архитектурных изменений
- При анализе trade-offs между решениями

### Spec Workflow (для новых фич)

**Когда использовать:**
- Разработка новой функциональности (не багфикс)
- Добавление нового компонента системы
- Архитектурные изменения

**Процесс:**
1. `mcp__spec-workflow-mcp__spec-workflow-guide` - загрузить workflow
2. Requirements → Design → Tasks → Implementation
3. Документация через spec-workflow
4. Логирование через `mcp__spec-workflow-mcp__log-implementation`

### Cognee Integration (для системы памяти)

**Когда использовать:**
- Добавление новой информации в knowledge graph
- Поиск по базе знаний
- Анализ архитектуры кодовой базы

**Основные операции:**
- `mcp__cognee__cognify` - процессинг данных в knowledge graph
- `mcp__cognee__search` - поиск по графу знаний
- `mcp__cognee__codify` - анализ кодовой базы

---

## 🔄 БАЗОВЫЙ ЦИКЛ РАБОТЫ

```
0. АНАЛИЗ (если сложная задача)
   Я → sequential-thinking → Обдумываю подход
   Я → Формирую стратегию

1. ИССЛЕДОВАНИЕ
   Я → Task(Explore/general-purpose) → "Диагностируй проблему X"
   Агент → (может использовать sequential-thinking если сложно)
   Агент → Исследует, НЕ меняет код
   Агент → Возвращает отчет (средняя детализация)

2. ПЛАНИРОВАНИЕ
   Я → sequential-thinking → Анализирую отчет агента
   Я → Формирую план действий
   Я → Показываю пользователю план (если критично)

3. РЕАЛИЗАЦИЯ
   Я → Task(general-purpose) → "Реализуй план в /home/dev/zapomni"
   Агент → Изменяет файлы локально
   Агент → Возвращает отчет о сделанных изменениях

4. CODE REVIEW
   Я → Task(general-purpose, новый) → "Проведи code review изменений"
   Агент → Проверяет качество, безопасность, best practices
   Агент → Возвращает отчет с замечаниями

5. ДОРАБОТКА (если review нашел проблемы)
   Возврат к шагу 3 (реализация)
   Цикл повторяется до успешного review

6. GIT COMMIT (только после согласования)
   Я → Спрашиваю разрешение на commit + push
   Пользователь → Подтверждает
   Я → Task(general-purpose) → "Commit и push в main"
   Агент → git add → commit → push origin main
   Агент → Возвращает статус

7. ВАЛИДАЦИЯ
   Я → Task(general-purpose) → "Проверь что все работает"
   Агент → Запускает тесты (если есть)
   Агент → Возвращает статус

8. ПРИНЯТИЕ РЕШЕНИЯ
   Я → Показываю результаты пользователю
   Пользователь → Решает что дальше
```

---

## 📋 ПРАВИЛА ДЛЯ АГЕНТОВ (детализация)

### Q1: Детализация отчетов
**Средние отчеты** (20-30 строк):
- Что исследовали
- Что нашли (проблема)
- Что рекомендуют (решение)
- Ключевые команды/файлы
- Статус (решена/не решена)

### Q2: Если агент не нашел решение
**Запускаю второго агента:**
```
Task(general-purpose, "КОНТЕКСТ: Предыдущий агент исследовал проблему X
и вернул такой отчет: [отчет агента 1].

ТВОЯ ЗАДАЧА: Подойди с ДРУГОЙ стороны:
- Используй web-поиск для поиска похожих проблем
- Проверь документацию Cognee, Claude Context, MCP
- Попробуй альтернативные подходы
- Думай нестандартно

ВАЖНО: Первый агент уже проверил [что он проверял], тебе нужно искать в другом месте.")
```

### Q3: Code Review
**ВСЕГДА review для:**
- Любые изменения в коде Python/JS/TypeScript
- Изменения в конфигурации MCP серверов
- Изменения в steering documents
- Изменения в шаблонах spec-workflow
- Интеграции с внешними системами

**Можно пропустить review для:**
- Документация (README.md)
- Комментарии в коде
- .gitignore, простые конфиги

### Q4: Code Review агент
**Новый агент** (свежий взгляд):
```
Task(general-purpose, "CODE REVIEW задача.

КОНТЕКСТ: Агент только что изменил следующие файлы локально:
[список файлов]

ИЗМЕНЕНИЯ: [краткое описание]

ТВОЯ ЗАДАЧА:
1. Прочитай измененные файлы (Read tool)
2. Проверь:
   - Безопасность (injection, XSS, path traversal)
   - Best practices для MCP/Python/JS
   - Нет ли breaking changes
   - Правильность интеграций
   - Обратная совместимость
3. Верни отчет:
   ✅ OK для commit
   ⚠️ Замечания (некритично)
   ❌ БЛОКИРУЕТ (критичные проблемы)

НЕ меняй код, только анализируй.")
```

### Q5: Параллелизация
**Всегда параллельно когда:**
- Несколько независимых багов
- Исследование разных компонентов
- Диагностика разных модулей

**Пример:**
```python
# ПРАВИЛЬНО - один вызов с несколькими Task
Task(Explore, "Проверь MCP серверы"),
Task(Explore, "Проверь Cognee integration"),
Task(Explore, "Проверь spec-workflow")

# НЕПРАВИЛЬНО - последовательно
Task(Explore, "Проверь MCP серверы")
# ждем результат
Task(Explore, "Проверь Cognee integration")
```

### Q6: Лимит агентов
**До 3 агентов параллельно** (оптимально для производительности)

### Q7: Согласование
**ВСЕГДА спрашивать разрешение:**
- Git push в main ветку
- Удаление файлов или данных
- Архитектурные изменения
- Изменения в steering documents (product.md, tech.md, structure.md)
- Prune операции в Cognee (очистка knowledge graph)

**Можно автоматически:**
- Диагностика и чтение файлов
- Code review
- Локальные изменения в /home/dev/zapomni (без commit)
- Поиск через Cognee
- Создание спецификаций через spec-workflow

### Q8: Если возникла проблема
**Показываю проблему:**
```
❌ ПРОБЛЕМА

Что произошло: [краткое описание]
Где: [файл:строка или компонент]

Логи/Ошибка:
[последние 30 строк]

Что делать?
A) Запустить агента для исправления
B) Откатить изменения
C) Другое (опиши)
```

### Q9: Типы агентов
**Explore** - быстрый поиск:
- Найти файлы по паттерну
- Grep по ключевым словам
- Быстрая диагностика (< 2 минуты)

**general-purpose** - полноценная работа:
- Сложная диагностика
- Изменение кода
- Многошаговые операции
- Code review
- Git операции

### Q10: Таймауты
**Ждать 10 минут** максимум для одного агента

Если агент работает > 10 минут:
```
⚠️ Агент "[описание задачи]" работает уже 10+ минут

Прервать или продолжить ждать?
```

### Q11: Rollback
**Автоматический rollback + уведомление:**
```
🔄 АВТОМАТИЧЕСКИЙ ROLLBACK ВЫПОЛНЕН

Причина: [описание проблемы]
Откачено к: commit abc123f
Статус: Локальная версия восстановлена

Проблема в новом коде: [описание]

Нужно исправить и попробовать заново?
```

---

## 📝 ШАБЛОНЫ ПРОМПТОВ ДЛЯ АГЕНТОВ

### 1. ДИАГНОСТИКА (Explore/general-purpose)

```
ЗАДАЧА: Исследовать проблему [описание]

КОНТЕКСТ ПРОЕКТА:
- Проект: Zapomni - локальная MCP система памяти для AI-агентов
- Локальный путь: /home/dev/zapomni
- Основные компоненты:
  * MCP серверы (Cognee, spec-workflow, sequential-thinking, SSH)
  * Spec Workflow система для документации
  * Steering documents (product.md, tech.md, structure.md)
  * Шаблоны для спецификаций

ПРОБЛЕМА: [детальное описание]

ИНСТРУМЕНТЫ (используй когда нужно):
🧠 mcp__sequential-thinking__sequentialthinking - если проблема сложная:
   - >3 возможных причин
   - Нужен пошаговый анализ
   - Trade-offs между решениями

🔍 WebSearch - если похожая проблема может быть известна:
   - Ошибки MCP протокола
   - Best practices Cognee
   - Known issues в библиотеках

🧬 Cognee Search - для поиска в knowledge graph:
   - mcp__cognee__search - поиск по проектной документации
   - Может содержать предыдущий опыт решения похожих проблем

ЧТО ДЕЛАТЬ:
1. (Опционально) Используй sequential-thinking для анализа проблемы
2. Читай локальные файлы через Read tool
3. Используй Grep для поиска по кодовой базе
4. Если нужно - используй WebSearch для поиска похожих проблем
5. Проверь Cognee knowledge graph (если проблема может быть задокументирована)
6. Найди root cause
7. Верни МНЕ отчет (средняя детализация):
   - Что проверил
   - Что нашел (root cause)
   - Что рекомендуешь исправить
   - Примеры команд/кода для воспроизведения

ВАЖНО - НЕ ДЕЛАЙ:
❌ НЕ изменяй код
❌ НЕ создавай файлы
❌ НЕ коммить
✅ ТОЛЬКО чтение и диагностика

ФОРМАТ ОТЧЕТА:
## ПРОБЛЕМА
[описание]

## ДИАГНОСТИКА
[что проверил, какие инструменты использовал]

## ROOT CAUSE
[точная причина]

## РЕКОМЕНДАЦИИ
[что нужно исправить, где, как]
```

### 2. РЕАЛИЗАЦИЯ (general-purpose)

```
ЗАДАЧА: Реализовать изменения согласно плану

ТИП ЗАДАЧИ: [bug_fix | new_feature | refactoring | documentation]

КОНТЕКСТ ПРОЕКТА:
- Локальный репозиторий: /home/dev/zapomni
- Ветка для работы: main
- НЕ коммитить, НЕ пушить - только изменить файлы
- После реализации будет code review

ПЛАН ИЗМЕНЕНИЙ:
[детальный план что менять]

ИНСТРУМЕНТЫ (используй по типу задачи):

🧠 sequential-thinking - для сложных решений:
   - Архитектурные изменения (новые MCP серверы, интеграции)
   - Рефакторинг большого кода
   - Выбор между несколькими подходами
   - Изменения в системе памяти/знаний

📋 spec-workflow - для НОВЫХ фич (не багфикс):
   1. mcp__spec-workflow-mcp__spec-workflow-guide - загрузи workflow
   2. Создай requirements.md
   3. Создай design.md
   4. Создай tasks.md
   5. Реализуй по tasks
   6. Логируй через mcp__spec-workflow-mcp__log-implementation

🧬 Cognee - для работы с knowledge graph:
   - mcp__cognee__cognify - добавить новую документацию в граф
   - mcp__cognee__codify - проанализировать код репозитория

ЧТО ДЕЛАТЬ:

Если bug_fix:
1. Читай файлы (Read tool)
2. Изменяй (Edit tool)
3. Проверь синтаксис
4. Верни отчет

Если new_feature:
1. Используй spec-workflow (загрузи guide)
2. Создай спецификацию
3. Реализуй по spec
4. Залогируй implementation
5. Верни отчет

Если сложное архитектурное решение:
1. Используй sequential-thinking для планирования
2. Реализуй согласно продуманному плану
3. Верни отчет

ВАЖНО - РАБОТАЙ ТОЛЬКО ЛОКАЛЬНО:
✅ Путь: /home/dev/zapomni
❌ НЕ делай git commit/push (это сделает другой агент после review)
❌ НЕ удаляй файлы без явного указания
❌ НЕ меняй steering documents без sequential-thinking

ФОРМАТ ОТЧЕТА:
## РЕАЛИЗАЦИЯ ЗАВЕРШЕНА

Измененные файлы:
1. file1.py: [что изменил]
2. file2.py: [что изменил]

Новые файлы:
1. file3.py: [описание]

Статус: ✅ Готово к code review
```

### 3. CODE REVIEW (general-purpose, новый агент)

```
ЗАДАЧА: Code review изменений

КОНТЕКСТ:
- Проект: Zapomni MCP система
- Агент только что изменил код ЛОКАЛЬНО
- Файлы: [список файлов]
- Цель изменений: [описание]

ЧТО ПРОВЕРИТЬ:
1. Безопасность:
   - Path traversal (file operations)
   - Command injection (shell commands)
   - XSS (если есть web UI)
   - Безопасность MCP протокола

2. Best Practices:
   - MCP protocol correctness
   - Правильность использования Cognee API
   - Spec-workflow соблюдение структуры
   - Качество документации

3. Обратная совместимость:
   - Не ломаются существующие MCP интеграции
   - Steering documents консистентны
   - Шаблоны не сломаны

4. Качество кода:
   - Нет дублирования
   - Читаемость
   - Комментарии где нужно
   - Соответствие архитектуре

ЧТО ДЕЛАТЬ:
1. Read измененные файлы
2. Анализируй каждое изменение
3. Верни отчет с оценкой

ФОРМАТ ОТЧЕТА:
## CODE REVIEW РЕЗУЛЬТАТЫ

### ✅ ОДОБРЕНО (можно коммитить):
- [что хорошо сделано]

### ⚠️ ЗАМЕЧАНИЯ (некритично):
- [minor issues]

### ❌ БЛОКИРУЕТ (исправить перед commit):
- [critical issues]

ИТОГОВАЯ ОЦЕНКА: ✅ OK | ⚠️ OK с замечаниями | ❌ БЛОКИРУЕТ
```

### 4. GIT OPERATIONS (general-purpose)

```
ЗАДАЧА: Commit и push изменений в Git

КОНТЕКСТ:
- Локальный репозиторий: /home/dev/zapomni
- Code review пройден: ✅
- Текущая ветка: main
- Согласование пользователя получено: ✅

ЧТО ДЕЛАТЬ:
1. git status - проверь состояние
2. git add [список файлов]
3. git commit -m "[тип]: [описание]

   [детали изменений]

   🤖 Generated with Claude Code
   Co-Authored-By: Claude <noreply@anthropic.com>"
4. git push origin main
5. Верни статус

ВАЖНО:
- Commit message в формате: fix:/feat:/chore:/docs: [описание]
- Используй HEREDOC для commit message
- НЕ пуши без review и согласования

ФОРМАТ ОТЧЕТА:
## GIT ОПЕРАЦИЯ ЗАВЕРШЕНА

Commit: [hash] - [message]
Branch: main
Статус: ✅ pushed successfully

Измененные файлы:
[список файлов]
```

### 5. VALIDATION (general-purpose)

```
ЗАДАЧА: Проверить что изменения работают корректно

КОНТЕКСТ:
- Проект: Zapomni MCP система
- Изменения: [описание]
- Файлы: [список]

ЧТО ПРОВЕРИТЬ:
1. Синтаксис:
   - Python: pylint, mypy (если установлены)
   - JavaScript: eslint (если есть)

2. MCP серверы:
   - Cognee MCP работает
   - Spec-workflow MCP работает
   - Sequential-thinking MCP работает

3. Функциональность:
   - Новый функционал работает как ожидается
   - Старый функционал не сломан

4. Документация:
   - Steering documents актуальны
   - README обновлен если нужно

ЧТО ДЕЛАТЬ:
1. Запусти проверки синтаксиса (если есть инструменты)
2. Протестируй ключевую функциональность
3. Проверь что ничего не сломалось
4. Верни детальный отчет

ФОРМАТ ОТЧЕТА:
## VALIDATION РЕЗУЛЬТАТЫ

### ✅ УСПЕШНО:
- [что проверено и работает]

### ⚠️ ПРЕДУПРЕЖДЕНИЯ:
- [некритичные проблемы]

### ❌ ОШИБКИ:
- [критичные проблемы требующие исправления]

ИТОГОВЫЙ СТАТУС: ✅ All good | ⚠️ With warnings | ❌ Failed
```

---

## 🚫 СТРОГИЕ ЗАПРЕТЫ ДЛЯ АГЕНТОВ

### НИКОГДА не делать без согласования:

❌ **НЕТ git push без review и согласования:**
```
# ЗАПРЕЩЕНО:
git push origin main  # без review и подтверждения

# РАЗРЕШЕНО:
1. Code review пройден
2. Пользователь подтвердил
3. Тогда git push
```

❌ **НЕТ удаления без согласования:**
```
# ЗАПРЕЩЕНО:
rm file.py
mcp__cognee__prune  # очистка всего knowledge graph
mcp__cognee__delete  # удаление данных
```

❌ **НЕТ изменений steering documents без sequential-thinking:**
```
# ЗАПРЕЩЕНО:
Edit product.md  # без обдумывания
Edit tech.md     # без анализа влияния

# РАЗРЕШЕНО:
1. sequential-thinking об изменениях
2. Обсуждение с пользователем
3. Тогда изменение
```

### ✅ РАЗРЕШЕНО автоматически:

```
# Диагностика:
Read файлы
Grep по кодовой базе
WebSearch
Cognee search

# Локальная разработка:
Edit файлы локально
Write новые файлы
Создание specs через spec-workflow

# Только с явным разрешением:
git commit
git push
Удаление файлов/данных
Prune операции
```

---

## 📊 ПАРАЛЛЕЛИЗАЦИЯ

### Когда запускать параллельно:

**✅ ДА - параллельно (до 3 агентов):**
```python
# Независимые диагностики
Task(Explore, "Проверь MCP серверы"),
Task(Explore, "Проверь Cognee интеграцию"),
Task(Explore, "Проверь spec-workflow")

# Независимые фиксы
Task(general-purpose, "Исправь баг X"),
Task(general-purpose, "Исправь баг Y")

# Параллельный research
Task(general-purpose, "Исследуй Cognee best practices"),
Task(general-purpose, "Исследуй Claude Context API")
```

**❌ НЕТ - последовательно:**
```python
# Code review зависит от реализации
Task(general-purpose, "Реализуй фичу") → жду результат
Task(general-purpose, "Проверь код") → используя результат первого

# Push зависит от commit
Task(general-purpose, "Commit код") → жду
Task(general-purpose, "Push") → после commit

# Cognify зависит от подготовки данных
Task(general-purpose, "Подготовь документы") → жду
Task(general-purpose, "Cognify данные") → после подготовки
```

---

## 🎯 СПЕЦИАЛЬНЫЕ СЦЕНАРИИ

### Сценарий 1: Агент ничего не нашел

```
Агент 1 вернул: "Проверил файлы, все OK, проблему не нашел"

Я делаю:
Task(general-purpose, "КОНТЕКСТ: Агент 1 проверил [X] и не нашел проблему.

ТВОЯ ЗАДАЧА: Расширенная диагностика:
1. Используй WebSearch для поиска похожих проблем (MCP, Cognee, etc)
2. Проверь Cognee knowledge graph - может быть похожая проблема
3. Проверь документацию проектов-аналогов (claude-context, cognee-mcp)
4. Проверь GitHub issues похожих проектов
5. Подход с другой стороны

Верни детальную диагностику или признай что проблема сложная.")
```

### Сценарий 2: Review нашел критичные проблемы

```
Review агент вернул: ❌ БЛОКИРУЕТ - Path traversal vulnerability в file_handler.py:123

Я делаю:
1. Показываю ТЕБЕ проблему
2. Спрашиваю разрешение на исправление
3. После твоего "да" → Task(general-purpose, "Исправь path traversal...")
4. Повторный review
5. Если OK → Git commit (после согласования)
```

### Сценарий 3: Архитектурное изменение

```
Пользователь: "Давай добавим поддержку нового vector database"

Я делаю:
1. sequential-thinking:
   - Какие варианты vector DB? (Milvus, Weaviate, Chroma, Qdrant)
   - Как это повлияет на Cognee интеграцию?
   - Какие риски?
   - Обратная совместимость?

2. Показываю план пользователю
3. Жду подтверждения
4. Task(general-purpose, "Реализуй интеграцию [выбранной DB]")
5. Review
6. Commit (после согласования)
```

---

## 🔐 БЕЗОПАСНОСТЬ

### Критичные операции (ВСЕГДА спрашивать):

1. ❌ Удаление данных (rm, mcp__cognee__delete, mcp__cognee__prune)
2. ❌ Git push в main ветку
3. ❌ Изменение steering documents (product.md, tech.md, structure.md)
4. ❌ Архитектурные изменения
5. ❌ Изменения в MCP server конфигурациях

### Некритичные (можно автоматически):

1. ✅ Локальные изменения в /home/dev/zapomni (без commit)
2. ✅ Чтение файлов и диагностика
3. ✅ Code review
4. ✅ Создание specs через spec-workflow
5. ✅ Поиск через Cognee

---

## 📈 МЕТРИКИ УСПЕХА АГЕНТОВ

После каждой задачи агента я трекаю:
- ✅ Задача решена с первого раза
- ⚠️ Задача решена со второй попытки (второй агент)
- ❌ Задача не решена (эскалация к пользователю)

Цель: >80% задач решаются с первого раза

---

## 🔄 ИТОГОВЫЙ WORKFLOW (краткая версия)

```
ЗАДАЧА → Диагностика (агент) → План (я) → Согласование (если критично)
   ↓
Реализация (агент, локально) → Review (новый агент) → Исправления (если нужно)
   ↓
Согласование с пользователем → Commit/Push (агент) → Validation (агент)
   ↓
Success ✅ → Докладываю тебе
Issues ⚠️ → Rollback + Докладываю тебе
```

**Время на типичную задачу:** 5-15 минут (вместо 30-60 минут раньше)

---

## 📦 ПРОЕКТ-СПЕЦИФИЧНАЯ ИНФОРМАЦИЯ

### Zapomni Project Details

**Тип проекта:** Локальная MCP система памяти для AI-агентов

**Назначение:** Объединяет лучшее от Cognee и Claude Context для создания универсальной системы памяти и знаний для AI CLI инструментов (Codex CLI, Claude CLI)

**Локальный путь:** `/home/dev/zapomni`

**Git Strategy:** Локальное репо, коммиты в main (MVP фаза)

**Технологический стек:**
- Будет определен после research фазы
- Планируется поддержка локальных LLM
- Локальные базы данных (vector + graph)
- Бесплатные и open-source инструменты

**Ключевые компоненты:**
1. **MCP Servers:**
   - cognee (система знаний)
   - spec-workflow (документация)
   - sequential-thinking (reasoning)
   - mcp-ssh (удаленные операции)

2. **Spec Workflow:**
   - Templates в `.spec-workflow/templates/`
   - Specs в `.spec-workflow/specs/`
   - Steering docs в `.spec-workflow/steering/`

3. **Steering Documents:**
   - `product.md` - видение продукта
   - `tech.md` - техническая архитектура
   - `structure.md` - структура проекта

**Окружения:**
- Только локальная разработка (пока нет prod/test серверов)

**CI/CD:**
- Пока нет автоматического деплоя
- Git операции только после review и согласования

**Критичные файлы для review:**
- Любые Python/JS/TS файлы
- MCP server конфигурации
- Steering documents
- Spec-workflow шаблоны

**Согласование требуется для:**
- Git push в main
- Удаление файлов/данных
- Архитектурные изменения
- Изменения steering documents

---

## 🚀 КАК ИСПОЛЬЗОВАТЬ В НОВОЙ СЕССИИ

### Для Claude Code (меня) в начале работы:

```
1. Пользователь: "Продолжаем работу над Zapomni"

2. Я ДЕЛАЮ:
   - Read(/home/dev/zapomni/AGENT_WORKFLOW.md)
   - Загружаю все правила в память
   - Отвечаю: "✅ Agent workflow загружен. Работаю через Task tool."

3. Начинаю работу согласно правилам:
   - Сложная задача → sequential-thinking сначала
   - Простая диагностика → Task(Explore)
   - Реализация → Task(general-purpose)
   - Review → Task(general-purpose, новый агент)
   - И так далее...
```

### Для обновления workflow:

```
Если правила изменились:
1. Edit(AGENT_WORKFLOW.md) - обновить правила
2. git add AGENT_WORKFLOW.md
3. git commit -m "docs: Update agent workflow rules - [что изменилось]"
4. git push origin main (ПОСЛЕ СОГЛАСОВАНИЯ)
```

### Quick Reference Card:

```
ДИАГНОСТИКА → Explore (быстро) или general-purpose (сложно)
РЕАЛИЗАЦИЯ → general-purpose + spec-workflow (если new_feature)
REVIEW → general-purpose (новый агент)
GIT OPS → general-purpose (только после согласования)
THINKING → sequential-thinking (я сам, перед планированием)
COGNEE → mcp__cognee__* tools для work с knowledge graph
```

---

## 🎓 ПРИМЕРЫ ПРОМПТОВ (ХОРОШИЕ vs ПЛОХИЕ)

### ❌ ПЛОХОЙ промпт:

```
Task(general-purpose, "Исправь систему памяти")

Проблемы:
- Слишком общее
- Нет контекста
- Агент не знает что делать
- Может наломать дров
```

### ✅ ХОРОШИЙ промпт:

```
Task(general-purpose, "КОНТЕКСТ: Zapomni MCP система, Cognee интеграция не работает при попытке cognify данных.

ДИАГНОСТИКА (только чтение):
1. Проверь файлы конфигурации MCP (если есть)
2. Читай логи Cognee (если есть)
3. Проверь examples в Cognee GitHub repo
4. WebSearch для известных проблем Cognee MCP
5. Проверь наличие необходимых зависимостей

НЕ ДЕЛАЙ:
- НЕ изменяй код
- НЕ удаляй файлы

ОТЧЕТ:
- Root cause проблемы
- Какой файл/строка
- Что нужно исправить")
```

---

## 📌 CHANGELOG

- 2025-11-22 v2.0: Адаптирован для Zapomni MCP проекта
  - Убрана вся специфика NextKargo
  - Добавлены MCP и Cognee workflows
  - Обновлены правила для локальной разработки
  - Добавлена поддержка spec-workflow и sequential-thinking
- Автор: Tony + Claude Code

---

**Этот документ - living document. Обновляется по мере развития проекта Zapomni.**

**ВАЖНО:** В начале каждой новой сессии - Claude Code должен прочитать этот файл!
