Quick SHASHKA status check.

First: Read `.shashka/config.yaml` for `language.communication`.
Output status in that language.

Read: `.shashka/state/SNAPSHOT.md` and `.shashka/state/ACTIVE.md`
Count SPECs in each folder under `.shashka/specs/`
Check: `gh pr list` and `gh workflow list` if available

Output brief status with counts and active work.

$ARGUMENTS
